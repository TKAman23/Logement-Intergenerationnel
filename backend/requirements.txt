# ============================================================
# Backend Dependencies — Logement Intergénérationnel
# FastAPI + HuggingFace + Firebase Admin
# ============================================================

# --- Web Framework ---
fastapi==0.111.0
uvicorn[standard]==0.29.0       # ASGI server with websocket support
python-multipart==0.0.9        # Required for file uploads in FastAPI

# --- Data Validation ---
pydantic==2.7.1                # FastAPI uses Pydantic v2 for models
pydantic-settings==2.2.1       # For loading .env config

# --- Firebase Admin SDK ---
firebase-admin==6.5.0          # Firestore, Auth, Storage access

# --- NLP / HuggingFace ---
transformers==4.41.0           # HuggingFace model hub + pipeline
torch==2.3.0+cu118             # PyTorch backend for transformers (CPU)
# NOTE: Using CPU-only torch to keep Render free tier viable.
# If you have a GPU, replace with: torch==2.3.0+cu118

# --- Async HTTP (for any outbound calls) ---
httpx==0.27.0

# --- CORS & Security ---
python-jose[cryptography]==3.3.0  # JWT handling (Firebase token verification)

# --- Dev / Testing ---
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0                  # Also used as async test client
